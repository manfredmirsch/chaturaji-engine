[package]
name    = "chaturaji-engine"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
chaturaji-core = { path = "../core" }
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
chaturaji-trainer = { path = "../trainer" }
